"use server";

// Stub - contact form not needed for the landing page
export async function submitContactInquiry(_data: unknown) {
  return { success: false, error: "Contact form is currently unavailable." };
}
