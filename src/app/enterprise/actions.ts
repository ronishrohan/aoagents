"use server";

// Stub - enterprise form not needed for the landing page
export async function submitEnterpriseInquiry(_data: unknown) {
  return { success: false, error: "Enterprise form is currently unavailable." };
}
